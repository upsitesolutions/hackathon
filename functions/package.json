{
  "name": "hackathon-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Azure Functions app for hackathon backend endpoints.",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prestart": "npm run build",
    "start": "func start",
    "start:verbose": "func start --verbose",
    "test": "echo \"No tests configured yet\""
  },
  "dependencies": {
    "@azure/cosmos": "^4.9.3",
    "@azure/functions": "^4.6.0",
    "@google/generative-ai": "^0.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
